[package]
name = "mingli-liuren"
description = "Da Liu Ren: the heaven plate rotated over the earth plate, stem lodging, the four courses and the three transmissions, with the school divergences left declared rather than invented."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["liuren", "divination", "chinese", "cyclic"]
categories = ["algorithms", "date-and-time"]

[dependencies]
serde_json = { workspace = true }
mingli-contract = { workspace = true }
mingli-astro = { workspace = true }
mingli-ganzhi = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
criterion = { version = "0.8", default-features = false }

[lints]
workspace = true

[[bench]]
name = "liuren_bench"
harness = false
