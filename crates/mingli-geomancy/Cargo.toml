[package]
name = "mingli-geomancy"
description = "Ilm al-raml geomancy: four mother figures seeded, then daughters, nieces, witnesses and the judge. The judge is always even, by a GF(2) parity theorem."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["geomancy", "raml", "divination", "gf2"]
categories = ["algorithms", "simulation"]

[dependencies]
serde_json = { workspace = true }
mingli-contract = { workspace = true }
mingli-core = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
criterion = { version = "0.8", default-features = false }

[lints]
workspace = true

[[bench]]
name = "geomancy_bench"
harness = false
