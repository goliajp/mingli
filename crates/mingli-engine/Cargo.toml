[package]
name = "mingli-engine"
description = "编排层：共享上下文记忆化 + rayon 并行 fan-out。注册表由调用方注入，本层不认识任何具体叶。"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
mingli-contract = { workspace = true }
serde_json = { workspace = true }
rayon = "1"

[lints]
workspace = true
