[package]
name = "mingli-analysis"
description = "Information-theoretic statistics across divination systems: entropy, mutual information and normalized mutual information over a shared input grid."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["information-theory", "entropy", "nmi", "statistics"]
categories = ["algorithms", "mathematics", "science"]

[dependencies]
mingli-contract = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
mingli-registry = { workspace = true }
criterion = { version = "0.8", default-features = false }

[lints]
workspace = true

[[bench]]
name = "analysis_bench"
harness = false
