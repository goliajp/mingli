[package]
name = "mingli-cartomancy"
description = "L3 叶(C 族)：抽牌占卜——塔罗/卢恩等无放回抽取 + 正逆位。建于 core(sampler 洗牌 Fisher-Yates 种子可复现)。牌名属🟡查表，本叶只出抽取机制(置换/逆位 bit)。"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
serde_json = { workspace = true }
mingli-contract = { workspace = true }
mingli-core = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
criterion = { version = "0.8", default-features = false }

[lints]
workspace = true

[[bench]]
name = "cartomancy_bench"
harness = false
